run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - gocyclo
    - gofmt
    - goimports
    - golint
    - unparam
    - deadcode

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
        - govet